import { createAction } from '@reduxjs/toolkit';
//  clearAllUsers is action type

// export const clearAllUsers(import) = createAction('clearAllUsers')(action type); it can be different

export const clearAllUsers = createAction('clearAllUsers');